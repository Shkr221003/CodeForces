#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        string s;
        cin >> s;

        int odd = 0;

        for (char c : s)
            if ((c - '0') & 1)
                odd++;

        cout << odd << '\n';
    }

    return 0;
}
