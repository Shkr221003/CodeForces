#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--) {
        int x;
        cin >> x;

        int a[4];

        for (int i = 1; i <= 3; i++)
            cin >> a[i];

        if (a[x] != 0 && a[a[x]] != 0)
            cout << "YES\n";
        else
            cout << "NO\n";
    }

    return 0;
}
