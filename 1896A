#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        int n;
        cin >> n;

        int x;
        cin >> x;

        for (int i = 1, y; i < n; i++) cin >> y;

        cout << (x == 1 ? "YES" : "NO") << '\n';
    }

    return 0;
}
