#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--) {
        int n, m;
        cin >> n >> m;

        int ans = 0;
        int used = 0;

        for (int i = 0; i < n; i++) {
            string s;
            cin >> s;

            if (used + (int)s.size() <= m) {
                used += s.size();
                ans++;
            }
        }

        cout << ans << '\n';
    }

    return 0;
}
