#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        long long a, b;
        cin >> a >> b;

        if (a & 1) a--;
        if (b & 1) b--;

        cout << a / 2 + b / 2 << '\n';
    }
    return 0;
}
