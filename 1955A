#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        long long n, a, b;
        cin >> n >> a >> b;

        cout << (n / 2) * min(b, 2 * a) + (n % 2) * a << '\n';
    }

    return 0;
}
