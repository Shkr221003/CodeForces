#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;

    int x = min(n, m);

    long long ans = 1;

    for (int i = 1; i <= x; i++) {
        ans *= i;
    }

    cout << ans << '\n';

    return 0;
}
