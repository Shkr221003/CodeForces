#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    int ans = 1e9, x;

    for (int i = 0; i < n; i++) {
        cin >> x;
        ans = min(ans, abs(x));
    }

    cout << ans;
    return 0;
}
