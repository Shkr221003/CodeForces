#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        long long n;
        cin >> n;

        long long p = 1;
        while (p * 2 <= n) p *= 2;

        cout << p - 1 << '\n';
    }

    return 0;
}
