#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        int n;
        cin >> n;

        long long sum = 0, x;
        for (int i = 0; i < n; i++) {
            cin >> x;
            sum += x;
        }

        long long r = sqrt(sum);
        cout << (r * r == sum ? "YES" : "NO") << '\n';
    }

    return 0;
}
